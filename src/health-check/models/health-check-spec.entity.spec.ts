import { HealthCheckSpecEntity } from './health-check-spec.entity';

describe('HealthCheckSpecEntity', () => {
  it('should be defined', () => {
    expect(new HealthCheckSpecEntity()).toBeDefined();
  });
});
