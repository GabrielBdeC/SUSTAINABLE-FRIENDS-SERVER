import { HealthCheckChildDto } from './health-check-child.dto';

describe('HealthCheckChildDto', () => {
  it('should be defined', () => {
    expect(new HealthCheckChildDto()).toBeDefined();
  });
});
