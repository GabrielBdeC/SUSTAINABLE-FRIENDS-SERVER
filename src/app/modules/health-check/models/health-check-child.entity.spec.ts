import { HealthCheckChildEntity } from './health-check-child.entity';

describe('HealthCheckChildEntity', () => {
  it('should be defined', () => {
    expect(new HealthCheckChildEntity()).toBeDefined();
  });
});
